[package]
name = "steal_stickers_bot"
version = "0.1.0"
edition = "2021"

[dependencies]
telers = { version = "1.0.0-alpha.20", features = ["memory-storage"]}
tokio = { version = "1.36", features = ["macros"]}
serde_json = "1.0"

random-string = "1.1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] } 